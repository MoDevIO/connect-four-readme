name: Do Move

on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: write

jobs:
  do-move:
    if: startsWith(github.event.issue.title, 'do_move:')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract move
        id: move
        env:
          TITLE: ${{ github.event.issue.title }}
        run: |
          if [[ "$TITLE" =~ ^do_move:([0-9]+)$ ]]; then
            echo "value=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
          else
            echo "Invalid issue title: $TITLE"
            exit 1
          fi
      
      - name: Close issue
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue close "${{ github.event.issue.number }}"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Run script
        run: |
          node src/engine.js "${{ steps.move.outputs.value }}"
          node src/drawBoard.js

      - name: Commit board changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/moves.txt data/valid_moves.txt data/row*.svg
          if [ -d data/recent ]; then
            git add data/recent/
          fi

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "do_move: ${{ steps.move.outputs.value }}"

      - name: Update README image URLs
        run: node src/updateIds.js "$(git rev-parse HEAD)"

      - name: Commit README changes
        run: |
          git add README.md

          if git diff --cached --quiet; then
            echo "No README changes to commit"
            exit 0
          fi

          git commit -m "chore: update README image URLs"
          git push
